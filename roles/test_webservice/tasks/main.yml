---
- name: Restart Web Service IIS
  win_iis_website:
    name: '{{ item }}'
    state: restarted
  with_items: "{{ webservice }}"

- name: Test web service
  win_uri:
    url: '{{ item }}'
    validate_certs: no
    status_code: 200
  with_items: "{{ url_wsdl }}"
